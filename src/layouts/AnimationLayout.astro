---
import HeadLayout from "./HeadLayout.astro";

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<HeadLayout title={title} description={description}>
  <div slot="loader" class="loader"></div>
  <slot />
</HeadLayout>
